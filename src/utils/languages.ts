// Langues actives par défaut d'un nouveau flow. Liste confirmée par l'équipe projet
// (context.md + QUESTIONS_STUDIO.md) : `fr` sert de langue de référence/débogage, les
// autres sont les langues locales effectivement diffusées. Cette liste n'est qu'une
// valeur par défaut — le Studio ne fige aucune langue dans le code, elle reste éditable
// librement dans les paramètres du flow (FlowSettingsManager).
export const DEFAULT_LANGUAGES = ['fr', 'fon', 'yoruba', 'dendi', 'adja'];
